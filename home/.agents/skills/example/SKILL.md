---
name: example
description: used to demostrate trivial skill usage
---

1) refer project root .agents/skills/example/CONFIG.md if has

2) then use `date` command to show user current date

3) report following uname data: !`uname -a`
